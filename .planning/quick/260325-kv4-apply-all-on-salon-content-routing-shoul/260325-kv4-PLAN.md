---
phase: quick
plan: 260325-kv4
type: execute
wave: 1
depends_on: []
files_modified:
  - src/routes/integrations.js
autonomous: true
requirements: []
must_haves:
  truths:
    - "Clicking Apply All toggles all checkboxes in that column visually but does NOT submit the form"
    - "Toggling an individual Content Routing checkbox does NOT auto-submit the form"
    - "Clicking the Save Routing button submits the form and persists changes"
  artifacts:
    - path: "src/routes/integrations.js"
      provides: "Content Routing card with manual-save-only behavior"
  key_links: []
---

<objective>
Remove auto-submit behavior from the salon Content Routing card on the Integrations page. Currently, both individual checkbox toggles and the Apply All toggle auto-submit the form. They should only change visual state; the explicit "Save Routing" button is the only way to persist changes.

Purpose: Prevent accidental saves when exploring routing options.
Output: Updated integrations.js with no auto-submit on checkboxes.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@src/routes/integrations.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove auto-submit from individual checkboxes and Apply All handler</name>
  <files>src/routes/integrations.js</files>
  <action>
Three changes in src/routes/integrations.js:

1. **Line ~231 — Remove onchange auto-submit from individual toggle cells.**
   In the `toggleCell()` function, the checkbox input has:
   ```
   onchange="if(!window.__applyAllActive)this.form.submit()"
   ```
   Remove the entire `onchange` attribute. The checkbox should have no onchange handler at all. The `col-${platform}` class must remain (used by Apply All to find column checkboxes).

2. **Lines ~772-774 — Remove form.submit() from the Apply All change handler.**
   In the `document.addEventListener('change', ...)` block that handles `data-apply-all` inputs, delete these three lines:
   ```js
   // Submit the routing form once
   var form = document.querySelector('form[action="/manager/integrations/routing-update"]');
   if (form) form.submit();
   ```
   Keep the rest of the handler intact (the checkbox cascade logic that sets `window.__applyAllActive` and toggles `col-{platform}` checkboxes).

3. **Clean up the __applyAllActive guard** — since individual checkboxes no longer auto-submit, the `window.__applyAllActive` flag is no longer needed to suppress cascading submits. However, it is harmless to leave, so optionally remove the `window.__applyAllActive = true/false` lines and the `if(!window.__applyAllActive)` guard for cleanliness. If removing, also remove the `onchange` attribute entirely (already done in step 1).

The "Save Routing" button (line ~631-634) already exists as `<button type="submit">` inside the form — it will continue to work as the only way to persist changes.
  </action>
  <verify>
    <automated>cd /Users/troyhardister/chairlyos/mostlypostly/mostlypostly-app && node -e "
      const fs = require('fs');
      const src = fs.readFileSync('src/routes/integrations.js', 'utf8');
      const hasOnchange = src.includes('this.form.submit()');
      const hasFormSubmitInApplyAll = /data-apply-all[\s\S]{0,500}form\.submit\(\)/.test(src);
      const hasSaveButton = src.includes('Save Routing');
      console.log('No onchange auto-submit:', !hasOnchange);
      console.log('No form.submit in Apply All:', !hasFormSubmitInApplyAll);
      console.log('Save Routing button exists:', hasSaveButton);
      if (hasOnchange || hasFormSubmitInApplyAll || !hasSaveButton) process.exit(1);
      console.log('ALL CHECKS PASSED');
    "
    </automated>
  </verify>
  <done>Individual checkbox changes and Apply All toggle only affect visual state. The Save Routing button is the sole mechanism to persist Content Routing changes.</done>
</task>

</tasks>

<verification>
- Toggle individual checkboxes on the Content Routing card: no page reload, no form submission
- Click Apply All: all column checkboxes toggle visually, no page reload
- Click Save Routing: form submits, changes persist, page reloads with routing=saved
</verification>

<success_criteria>
- Zero auto-submit behavior on any checkbox in the Content Routing card
- Save Routing button remains the only way to persist changes
- Apply All still toggles all checkboxes in its column visually
</success_criteria>

<output>
After completion, create `.planning/quick/260325-kv4-apply-all-on-salon-content-routing-shoul/260325-kv4-SUMMARY.md`
</output>
