---
phase: quick-260319-i8s
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/core/vendorScheduler.js
  - src/scheduler.js
autonomous: true
requirements: [QUICK-i8s]
must_haves:
  truths:
    - "Vendor posts with affiliate URL get 'Shop today: {tracked short URL}' in FB caption between AI copy and hashtag block"
    - "Vendor posts with affiliate URL get 'Shop, link in bio.' in IG caption with all URLs stripped"
    - "Vendor posts without affiliate URL behave exactly as before (no shop CTA, no IG treatment)"
    - "AI prompt no longer receives affiliate URL — placement is deterministic"
  artifacts:
    - path: "src/core/vendorScheduler.js"
      provides: "Deterministic affiliate URL injection in processCampaign, clean AI prompt"
    - path: "src/scheduler.js"
      provides: "IG-specific caption derivation for vendor posts at publish time"
  key_links:
    - from: "src/core/vendorScheduler.js"
      to: "src/core/trackingUrl.js"
      via: "buildTrackingToken + buildShortUrl for affiliate short URL"
      pattern: "buildTrackingToken.*clickType.*vendor"
    - from: "src/scheduler.js"
      to: "posts.final_caption"
      via: "IG caption derived on-the-fly from stored FB caption"
      pattern: "replace.*Shop today.*Shop, link in bio"
---

<objective>
Make vendor post affiliate URL injection deterministic: the system controls URL placement (not the AI), FB gets a tracked shop CTA, IG gets "Shop, link in bio." with URLs stripped.

Purpose: AI was unreliably including/mangling affiliate URLs. Moving to system-controlled placement ensures consistent tracking and platform-appropriate formatting.
Output: Updated vendorScheduler.js and scheduler.js
</objective>

<execution_context>
@/Users/troyhardister/.claude/get-shit-done/workflows/execute-plan.md
@/Users/troyhardister/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@src/core/vendorScheduler.js
@src/scheduler.js
@src/core/trackingUrl.js
@src/core/composeFinalCaption.js
@CLAUDE.md — "Vendor affiliate URL" convention, "UTM tracking tokens" convention
</context>

<tasks>

<task type="auto">
  <name>Task 1: Deterministic affiliate URL in vendorScheduler.js</name>
  <files>src/core/vendorScheduler.js</files>
  <action>
Three changes to vendorScheduler.js:

**1. Remove affiliate URL from AI prompt (generateVendorCaption, line ~65):**
- Remove the `affiliateUrl` parameter from the function signature
- Delete the `safeAffiliateUrl` validation block (lines 76-88)
- Delete the `${safeAffiliateUrl ? ...}` line from `userPrompt` (line 109)
- The function signature becomes `generateVendorCaption({ campaign, salon })`

**2. Update the call site (processCampaign, line ~269):**
- Change `generateVendorCaption({ campaign, salon, affiliateUrl })` to `generateVendorCaption({ campaign, salon })`

**3. Replace the old "replace affiliateUrl in caption" block (lines 304-327) with deterministic shop CTA injection:**
- Remove the entire `if (affiliateUrl && finalCaption.includes(affiliateUrl))` block
- Instead, after building `lockedBlock` (line 291), build the final caption with this logic:

```
if (affiliateUrl) {
  const utmContent = `vendor_${slugify(campaign.vendor_name)}`;
  const destination = appendUtm(affiliateUrl, {
    source: 'mostlypostly',
    medium: 'social',
    campaign: salonId,
    content: utmContent,
  });
  try {
    const token = buildTrackingToken({
      salonId,
      postId,
      clickType: 'vendor',
      vendorName: campaign.vendor_name,
      utmContent,
      destination,
    });
    const shortUrl = buildShortUrl(token);
    trackedCaption = caption + "\n\nShop today: " + shortUrl + (lockedBlock ? "\n\n" + lockedBlock : "");
  } catch (err) {
    log.warn(`  UTM token creation failed for campaign ${campaign.id}: ${err.message}`);
    trackedCaption = caption + (lockedBlock ? "\n\n" + lockedBlock : "");
  }
} else {
  trackedCaption = caption + (lockedBlock ? "\n\n" + lockedBlock : "");
}
```

Note: `postId` is already declared on line 300 (`const postId = crypto.randomUUID()`), so it is available. The old `finalCaption` variable (line 291) should be removed — `trackedCaption` replaces it entirely. Update the DB insert to use `trackedCaption` for `final_caption` (it already does on line 349, just remove the now-unused `finalCaption` variable).
  </action>
  <verify>
    <automated>node -e "import('./src/core/vendorScheduler.js').then(m => { console.log('generateVendorCaption params:', m.generateVendorCaption.toString().match(/\{[^}]+\}/)[0]); console.log('buildVendorHashtagBlock:', typeof m.buildVendorHashtagBlock); console.log('OK'); })" 2>&1 | head -20</automated>
  </verify>
  <done>
- generateVendorCaption no longer accepts affiliateUrl param
- AI prompt has no affiliate URL injection line
- processCampaign builds "Shop today: {shortUrl}" deterministically when affiliateUrl is set
- Caption format: `{AI copy}\n\nShop today: {shortUrl}\n\n{hashtag block}`
- No affiliate URL = same behavior as before (AI copy + hashtags only)
  </done>
</task>

<task type="auto">
  <name>Task 2: IG-specific vendor caption at publish time in scheduler.js</name>
  <files>src/scheduler.js</files>
  <action>
In `runSchedulerOnce()`, inside the publish loop (the `for (const post of due)` block starting ~line 374), add vendor post IG caption derivation BEFORE the publish calls.

Insert this block right after the `allImages` array is resolved (after line ~438, before the `isMulti` check on line 445):

```
// --- Vendor post: derive platform-specific captions ---
const isVendorPost = !!post.vendor_campaign_id;
let fbCaption = post.final_caption;
let igCaption = post.final_caption;

if (isVendorPost) {
  // IG: strip "Shop today: URL" line, strip any remaining URLs, append "Shop, link in bio."
  igCaption = post.final_caption
    .replace(/\n\nShop today: https?:\/\/\S+/gi, '')
    .replace(/https?:\/\/\S+/gi, '')
    .replace(/\n{3,}/g, '\n\n')
    .trim();
  if (post.final_caption.includes('Shop today:')) {
    igCaption += '\n\nShop, link in bio.';
  }
}
```

Then update the THREE publish call sites to use the correct caption per platform:

1. **Multi-image FB** (line ~461): change `post.final_caption` to `fbCaption`
2. **Multi-image IG** (line ~463): change `post.final_caption` to `igCaption`
3. **Single-image FB** (line ~469): change `post.final_caption` to `fbCaption`
4. **Single-image IG** (line ~471): change `post.final_caption` to `igCaption`
5. **GMB caption** (line ~507): change `post.final_caption` to `fbCaption` (GMB gets the full URL like FB)

For non-vendor posts, `fbCaption` and `igCaption` are both `post.final_caption` so behavior is unchanged.

IMPORTANT: The story-only path (line ~451, for availability/promotions) does NOT use captions for IG stories, so no change needed there.
  </action>
  <verify>
    <automated>node -e "import('./src/scheduler.js').then(m => { console.log('runSchedulerOnce:', typeof m.runSchedulerOnce); console.log('enqueuePost:', typeof m.enqueuePost); console.log('OK'); })" 2>&1 | head -10</automated>
  </verify>
  <done>
- Vendor posts publish FB caption with "Shop today: {shortUrl}" intact
- Vendor posts publish IG caption with URL stripped and "Shop, link in bio." appended
- Non-vendor posts are completely unaffected (both captions = post.final_caption)
- GMB gets the FB caption (includes URL)
  </done>
</task>

</tasks>

<verification>
1. Module imports resolve: `node -e "import('./src/core/vendorScheduler.js').then(() => console.log('vendorScheduler OK'))"`
2. Module imports resolve: `node -e "import('./src/scheduler.js').then(() => console.log('scheduler OK'))"`
3. Grep confirms no affiliate URL in AI prompt: `grep -n "safeAffiliateUrl\|Include this partner link" src/core/vendorScheduler.js` should return nothing
4. Grep confirms Shop today injection: `grep -n "Shop today:" src/core/vendorScheduler.js` should show the deterministic block
5. Grep confirms IG derivation: `grep -n "Shop, link in bio" src/scheduler.js` should show the vendor IG block
</verification>

<success_criteria>
- Vendor posts with affiliate URL: FB caption = `{AI copy}\n\nShop today: {tracked short URL}\n\n{hashtags}`, IG caption = `{AI copy}\n\n{hashtags}\n\nShop, link in bio.`
- Vendor posts without affiliate URL: same as before (no shop CTA, no special IG treatment)
- Non-vendor posts: completely unchanged
- AI prompt is clean — no affiliate URL passed to GPT
</success_criteria>

<output>
After completion, create `.planning/quick/260319-i8s-inject-affiliate-url-into-vendor-posts-f/260319-i8s-SUMMARY.md`
</output>
