---
phase: quick
plan: 260320-dn3
type: execute
wave: 1
depends_on: []
files_modified:
  - src/routes/manager.js
autonomous: true
requirements: []
must_haves:
  truths:
    - "First 5 recent activity items are visible by default on dashboard load"
    - "Items 6+ are hidden behind a Show More toggle"
    - "Recent activity query is limited to a 2-week window"
    - "Clicking Show More reveals all remaining items; clicking Show Less collapses back to 5"
  artifacts:
    - path: "src/routes/manager.js"
      provides: "Collapsible recent activity section on manager dashboard"
  key_links: []
---

<objective>
Add a collapsible recent activity section to the manager dashboard. The first 5 items show expanded, remaining items are hidden behind a "Show more" toggle, and the query window is capped at 2 weeks.

Purpose: Reduce visual clutter on the dashboard while keeping recent context accessible.
Output: Updated manager dashboard with collapsible recent activity.
</objective>

<execution_context>
@/Users/troyhardister/.claude/get-shit-done/workflows/execute-plan.md
@/Users/troyhardister/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@src/routes/manager.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add collapsible recent activity with 2-week window</name>
  <files>src/routes/manager.js</files>
  <action>
In `src/routes/manager.js`, make three changes to the `router.get("/")` handler:

1. **Cap the recent posts query to 2 weeks** (line ~169-178): Change the existing `recentRaw` query to add a date filter. Replace the current query with:
```sql
SELECT *
  FROM posts
  WHERE salon_id = ?
    AND status NOT IN ('manager_pending', 'draft', 'cancelled')
    AND datetime(created_at) >= datetime('now', '-14 days')
  ORDER BY created_at DESC
  LIMIT 25
```
No new params needed — the `datetime('now', '-14 days')` is a SQLite literal.

2. **Split recent cards into visible (first 5) and collapsed (rest)** (around line ~283-344): After generating the `recentCards` markup from the `recent` array, split the rendering into two groups:
   - `recentVisible`: first 5 items from `recent` array, rendered as cards (same markup as today)
   - `recentCollapsed`: items 6+ from `recent` array, rendered as cards wrapped in a `<div id="recent-collapsed" style="display:none">` container

   Replace the single `recentCards` variable with two: `recentVisibleCards` (items 0-4) and `recentCollapsedCards` (items 5+). Use `.slice(0, 5)` and `.slice(5)` on the `recent` array, then `.map()` each with the existing card rendering function.

3. **Update the page body HTML** (around line ~497-498): Replace:
```
<h2 ...>Recent Activity</h2>
${recentCards}
```
With:
```html
<div class="flex items-center justify-between mt-10 mb-3">
  <h2 class="text-xl font-bold text-mpCharcoal">Recent Activity</h2>
  <span class="text-xs text-mpMuted">${recent.length} post${recent.length !== 1 ? 's' : ''} in the last 14 days</span>
</div>
${recentVisibleCards}
${recentCollapsedCards}
${recent.length > 5 ? `
  <button id="recent-toggle" type="button"
    class="mt-2 mb-4 text-xs font-semibold text-mpAccent hover:underline">
    Show ${recent.length - 5} more
  </button>
` : ''}
```

4. **Add toggle script** in the existing `<script>` block (around line ~507). Add this click handler inside the existing `document.addEventListener('click', ...)`:
```js
// Recent activity toggle
if (e.target.id === 'recent-toggle') {
  var collapsed = document.getElementById('recent-collapsed');
  if (!collapsed) return;
  var showing = collapsed.style.display !== 'none';
  collapsed.style.display = showing ? 'none' : '';
  e.target.textContent = showing
    ? 'Show ' + collapsed.children.length + ' more'
    : 'Show less';
  return;
}
```
Place this BEFORE the existing "Show more / show less" caption handler to avoid event conflicts.

Keep the empty-state message (`No recent posts.`) for when `recent.length === 0`.
  </action>
  <verify>
    <automated>cd /Users/troyhardister/chairlyos/mostlypostly/mostlypostly-app && node -e "import('./src/routes/manager.js').then(() => console.log('OK')).catch(e => { console.error(e); process.exit(1); })"</automated>
  </verify>
  <done>
    - Dashboard loads with first 5 recent items visible
    - Items 6+ are hidden in a collapsed div
    - "Show N more" button appears when there are more than 5 items
    - Clicking toggle reveals/hides collapsed items and updates button text
    - Recent activity query only fetches posts from the last 14 days
    - Count label shows "N posts in the last 14 days" next to the heading
  </done>
</task>

</tasks>

<verification>
- `node -e "import('./src/routes/manager.js')"` loads without errors
- Visual check: dashboard shows 5 recent cards, toggle button, 14-day count
</verification>

<success_criteria>
- Recent activity section shows max 5 items on load
- Toggle button expands/collapses remaining items
- Query window capped at 14 days
- No regressions to pending cards, failed banner, or promo sections
</success_criteria>

<output>
After completion, create `.planning/quick/260320-dn3-add-collapsible-recent-activity-section-/260320-dn3-SUMMARY.md`
</output>
