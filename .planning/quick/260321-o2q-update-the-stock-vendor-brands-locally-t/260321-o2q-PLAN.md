---
phase: quick
plan: 260321-o2q
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/seed-vendor-brands.mjs
autonomous: true
requirements: []
must_haves:
  truths:
    - "vendor_brands table contains exactly Aveda, Redken, and Olaplex"
    - "All related tables (vendor_campaigns, salon_vendor_feeds, salon_vendor_approvals) reference the new brand names"
    - "Brand hashtags are populated with real brand-appropriate hashtags"
  artifacts:
    - path: "scripts/seed-vendor-brands.mjs"
      provides: "Repeatable vendor brand seed script"
  key_links:
    - from: "vendor_brands.vendor_name"
      to: "vendor_campaigns.vendor_name, salon_vendor_feeds.vendor_name, salon_vendor_approvals.vendor_name"
      via: "UPDATE statements rename all FK references"
      pattern: "UPDATE.*SET vendor_name"
---

<objective>
Rename the three placeholder vendor brands ("Your Brand", "Your Brand 2", "Your Brand 3") to Aveda, Redken, and Olaplex in the local SQLite database (postly.db).

Purpose: Replace placeholder test data with real professional beauty brand names so the vendor feature looks realistic during demos and development.
Output: A seed script that renames brands across all related tables and populates brand-specific hashtags and categories.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md

Current state of vendor_brands table:
- "Your Brand" (7 campaigns, enabled feed for vanity-lounge, approved)
- "Your Brand 2" (campaigns exist, enabled feed, approved)
- "Your Brand 3" (campaigns exist, disabled feed, approved)

Related tables that reference vendor_name as a text FK:
- vendor_campaigns.vendor_name
- salon_vendor_feeds.vendor_name
- salon_vendor_approvals.vendor_name

vendor_post_log references campaigns by UUID (campaign_id), NOT by vendor_name — no update needed there.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create and run vendor brand rename script</name>
  <files>scripts/seed-vendor-brands.mjs</files>
  <action>
Create `scripts/seed-vendor-brands.mjs` — an ESM script that uses better-sqlite3 to update the local postly.db.

The script must do the following in a single transaction:

1. Rename vendor_brands rows (vendor_name is PK so must DELETE old + INSERT new):
   - "Your Brand" -> "Aveda"
   - "Your Brand 2" -> "Redken"
   - "Your Brand 3" -> "Olaplex"

   Since vendor_name is a TEXT PRIMARY KEY, use this approach per brand:
   - UPDATE vendor_campaigns SET vendor_name = 'Aveda' WHERE vendor_name = 'Your Brand'
   - UPDATE salon_vendor_feeds SET vendor_name = 'Aveda' WHERE vendor_name = 'Your Brand'
   - UPDATE salon_vendor_approvals SET vendor_name = 'Aveda' WHERE vendor_name = 'Your Brand'
   - INSERT INTO vendor_brands (vendor_name, brand_hashtags, categories, allow_client_renewal, product_value) VALUES ('Aveda', ...) with appropriate values
   - DELETE FROM vendor_brands WHERE vendor_name = 'Your Brand'
   (Repeat for each brand)

2. Set brand-specific metadata on the new vendor_brands rows:
   - Aveda: brand_hashtags = '["#AvedaColor","#Aveda"]', categories = '["Color","Standard","Promotion"]', product_value = 55
   - Redken: brand_hashtags = '["#RedkenReady","#Redken"]', categories = '["Color","Standard","Styling"]', product_value = 40
   - Olaplex: brand_hashtags = '["#Olaplex","#OlaplexTreatment"]', categories = '["Treatment","Standard","Repair"]', product_value = 50

3. Carry over existing column values for min_gap_days, platform_max_cap, allow_client_renewal from the old rows (or use defaults: min_gap_days=3, platform_max_cap=6, allow_client_renewal=1).

4. Print before/after state: SELECT * FROM vendor_brands at start and end.

Run the script with: `node scripts/seed-vendor-brands.mjs`

Import pattern: `import Database from 'better-sqlite3';` with path `path.join(process.cwd(), 'postly.db')` (matching db.js local env pattern).
  </action>
  <verify>
    <automated>node -e "import Database from 'better-sqlite3'; const db = new Database('postly.db'); const brands = db.prepare('SELECT vendor_name FROM vendor_brands ORDER BY vendor_name').all().map(r => r.vendor_name); const expected = ['Aveda','Olaplex','Redken']; console.log('Brands:', brands); if (JSON.stringify(brands) !== JSON.stringify(expected)) { console.error('FAIL: expected', expected); process.exit(1); } const feeds = db.prepare('SELECT vendor_name FROM salon_vendor_feeds ORDER BY vendor_name').all().map(r => r.vendor_name); console.log('Feeds:', feeds); if (feeds.some(n => n.startsWith('Your'))) { console.error('FAIL: feeds still have placeholder names'); process.exit(1); } console.log('PASS'); db.close();"</automated>
  </verify>
  <done>
    - vendor_brands contains exactly Aveda, Redken, Olaplex (no "Your Brand" rows)
    - vendor_campaigns reference new brand names
    - salon_vendor_feeds reference new brand names
    - salon_vendor_approvals reference new brand names
    - Brand hashtags and categories populated with realistic values
  </done>
</task>

</tasks>

<verification>
Query all four tables to confirm no "Your Brand" references remain and new brand names are present throughout.
</verification>

<success_criteria>
- `SELECT vendor_name FROM vendor_brands` returns exactly: Aveda, Olaplex, Redken
- `SELECT DISTINCT vendor_name FROM vendor_campaigns` returns only Aveda, Olaplex, Redken
- `SELECT vendor_name FROM salon_vendor_feeds` returns only Aveda, Olaplex, Redken
- No rows in any table contain "Your Brand" in vendor_name
</success_criteria>

<output>
After completion, create `.planning/quick/260321-o2q-update-the-stock-vendor-brands-locally-t/260321-o2q-SUMMARY.md`
</output>
