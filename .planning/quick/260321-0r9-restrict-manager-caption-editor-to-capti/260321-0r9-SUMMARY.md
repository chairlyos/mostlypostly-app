---
phase: quick-260321-0r9
plan: 260321-0r9
subsystem: manager-dashboard, scheduler, caption-pipeline
tags: [caption, manager-ux, publish, utm, composeFinalCaption]
dependency_graph:
  requires: []
  provides: [clean-caption-edit-ux, publish-time-caption-rebuild]
  affects: [src/routes/manager.js, src/scheduler.js]
tech_stack:
  added: []
  patterns: [composeFinalCaption-at-publish, base_caption-as-edit-source]
key_files:
  modified:
    - src/routes/manager.js
    - src/scheduler.js
decisions:
  - "Edit form shows base_caption — managers only touch creative content"
  - "Edit POST saves cleaned text to both base_caption and final_caption — scheduler uses base_caption at publish"
  - "UTM injection removed from enqueuePost — composeFinalCaption handles tracking URL via salonId+postId params"
  - "Vendor posts unchanged — keep final_caption path since vendorScheduler assembles their captions"
metrics:
  duration: 8 minutes
  completed: 2026-03-21
  tasks: 2
  files: 2
---

# Quick Task 260321-0r9: Restrict Manager Caption Editor to Caption-Only

**One-liner:** Manager edit textarea now shows only AI-generated caption text; hashtags, stylist credit, and booking URL are reattached by composeFinalCaption at publish time.

## What Was Done

### Task 1 — Manager edit form shows and saves only core caption

**manager.js changes:**

- Edit GET textarea value changed from `post.final_caption || post.caption` to `post.base_caption || post.caption`
- Helper note added below textarea: "Hashtags, stylist credit, and booking link are added automatically when posted."
- Edit POST now saves to both `base_caption` and `final_caption` (same cleaned text) so both columns stay consistent
- Dashboard pending cards: prefer `p.base_caption` over `p.final_caption` for display (fallback chain preserved)
- Dashboard recent cards: same preference update

**Commit:** `580f5da`

### Task 2 — Scheduler rebuilds full caption at publish time

**scheduler.js changes:**

- Replaced direct `post.final_caption` assignment for `fbCaption`/`igCaption` with `composeFinalCaption` calls
- Standard posts: `composeFinalCaption` called for both `platform: 'facebook'` and `platform: 'instagram'` using `post.base_caption || post.final_caption` as source
- Vendor posts: unchanged — continue using `post.final_caption` (assembled by `vendorScheduler.js`)
- Legacy UTM injection block removed from `enqueuePost` — `composeFinalCaption` handles tracking token injection via `salonId` + `postId` parameters
- Stale imports (`appendUtm`, `buildTrackingToken`, `buildShortUrl`) removed; `slugify` kept (still used for `stylistSlug`)

**Commits:** `02229d9`, `95e82e5`

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 1 - Bug] Removed stale imports after UTM injection removal**
- **Found during:** Task 2
- **Issue:** After removing the UTM injection block, `appendUtm`, `buildTrackingToken`, and `buildShortUrl` imports were no longer referenced in scheduler.js
- **Fix:** Removed the two stale import lines; kept `slugify` from utm.js (used in publish block for `stylistSlug`)
- **Files modified:** src/scheduler.js
- **Commit:** 95e82e5

## Commits

| Hash | Message |
|------|---------|
| 580f5da | feat(quick-260321-0r9): manager edit form shows and saves only core caption |
| 02229d9 | feat(quick-260321-0r9): rebuild FB/IG captions from base_caption at publish time |
| 95e82e5 | chore(quick-260321-0r9): remove stale imports after UTM injection removal |

## Self-Check: PASSED

Files confirmed modified:
- src/routes/manager.js — base_caption in edit form, helper note, dual-column save
- src/scheduler.js — composeFinalCaption for FB/IG, vendor path unchanged, UTM block removed

Commits confirmed present in git log: 580f5da, 02229d9, 95e82e5
