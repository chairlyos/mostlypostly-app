---
phase: 01-vendor-sync
plan: 04
type: execute
wave: 2
depends_on:
  - 01-02
files_modified:
  - src/routes/vendorAdmin.js
autonomous: true
requirements:
  - VSYNC-09

must_haves:
  truths:
    - "Platform Console shows Sync Now button per vendor brand"
    - "Platform Console shows last_sync_at timestamp, last_sync_count, and last_sync_error per vendor"
    - "POST /internal/vendors/sync/:vendorName triggers the async sync pipeline and returns immediately"
    - "Sync route uses requireSecret + requirePin authentication"
  artifacts:
    - path: "src/routes/vendorAdmin.js"
      provides: "Sync Now route + status display in Console UI"
  key_links:
    - from: "src/routes/vendorAdmin.js POST /sync/:vendorName"
      to: "src/core/vendorSync.js runVendorSync()"
      via: "import and async call"
      pattern: "runVendorSync"
    - from: "src/routes/vendorAdmin.js GET /"
      to: "vendor_brands table"
      via: "last_sync_at, last_sync_count, last_sync_error columns"
      pattern: "last_sync_at"
---

<objective>
Add the Sync Now button and sync status display to the Platform Console (vendorAdmin.js), plus the POST route that triggers the sync pipeline.

Purpose: Operators can trigger a vendor sync on-demand and see the result (last sync time, count, errors) without SSH access.
Output: Modified vendorAdmin.js with new route and UI section.
</objective>

<execution_context>
@/Users/troyhardister/.claude/get-shit-done/workflows/execute-plan.md
@/Users/troyhardister/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-vendor-sync/01-CONTEXT.md

<interfaces>
From src/routes/vendorAdmin.js — auth middleware pattern:
```javascript
function requireSecret(req, res, next) { /* checks req.query.secret === process.env.INTERNAL_SECRET */ }
function requirePin(req, res, next) { /* checks req.session.internal_pin_verified */ }
// All routes use: router.post("/path", requireSecret, requirePin, handler)
```

From src/core/vendorSync.js (Plan 02 output):
```javascript
export async function runVendorSync(vendorName) {
  // Returns { vendor, imported, skipped, error }
}
```

From vendor_brands table (after migration 045):
- last_sync_at TEXT
- last_sync_count INTEGER DEFAULT 0
- last_sync_error TEXT
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add sync route and Console UI section</name>
  <files>src/routes/vendorAdmin.js</files>
  <read_first>
    - src/routes/vendorAdmin.js (full file — need to find the brand detail page and the main dashboard rendering to add sync UI)
    - src/core/vendorSync.js (verify runVendorSync export signature)
  </read_first>
  <action>
1. **Add import** at top of vendorAdmin.js (after existing imports):
```javascript
import { runVendorSync } from '../core/vendorSync.js';
```

2. **Add POST route** for triggering sync (place near other POST routes, before `export default router`):
```javascript
router.post("/sync/:vendorName", requireSecret, requirePin, (req, res) => {
  const { vendorName } = req.params;

  // Verify vendor exists
  const brand = db.prepare('SELECT vendor_name FROM vendor_brands WHERE vendor_name = ?').get(vendorName);
  if (!brand) {
    return res.status(404).json({ error: 'Vendor not found' });
  }

  // Fire async — don't await (operator sees result on next page load via last_sync_at)
  runVendorSync(vendorName).catch(err => {
    console.error(`[VendorAdmin] Sync error for ${vendorName}:`, err.message);
  });

  res.json({ status: 'started', vendor: vendorName, message: 'Sync started — refresh page in a few minutes to see results' });
});
```

3. **Add sync status to the vendor brands page.** Find the GET route that renders the main Console dashboard (GET "/", around line 204). In the HTML rendering section where vendor brands are listed, for each brand add a "Sync Status" card section:

Find the section that iterates over brands to display them. After the existing brand info (brand_hashtags, categories, allow_client_renewal), add:

```html
<!-- Vendor Sync Status -->
<div class="mt-4 p-3 bg-gray-50 rounded border border-gray-200">
  <h4 class="font-semibold text-sm text-gray-700 mb-2">Automated Sync</h4>
  <div class="grid grid-cols-3 gap-2 text-sm mb-2">
    <div>
      <span class="text-gray-500">Last Sync:</span><br>
      <span class="font-medium">${brand.last_sync_at ? new Date(brand.last_sync_at).toLocaleString() : 'Never'}</span>
    </div>
    <div>
      <span class="text-gray-500">Campaigns Imported:</span><br>
      <span class="font-medium">${brand.last_sync_count || 0}</span>
    </div>
    <div>
      <span class="text-gray-500">Status:</span><br>
      <span class="font-medium ${brand.last_sync_error ? 'text-red-600' : 'text-green-600'}">${brand.last_sync_error ? 'Error' : (brand.last_sync_at ? 'OK' : 'Not synced')}</span>
    </div>
  </div>
  ${brand.last_sync_error ? `<div class="text-xs text-red-600 bg-red-50 p-2 rounded mb-2">${brand.last_sync_error}</div>` : ''}
  <form method="POST" action="/internal/vendors/sync/${encodeURIComponent(brand.vendor_name)}?secret=${encodeURIComponent(secret)}" class="inline">
    <button type="submit" class="px-3 py-1.5 bg-mpAccent text-white text-sm rounded hover:bg-blue-700 transition">
      Sync Now
    </button>
  </form>
</div>
```

4. **Update the brands query** in the GET "/" handler to include sync columns. Find the SELECT query for vendor_brands and ensure it includes `last_sync_at, last_sync_count, last_sync_error`. If it uses `SELECT *`, no change needed. If it lists specific columns, add them.

5. **Make the sync form work with JSON response.** Since the POST returns JSON but the form submits as HTML, convert the form to use a small inline fetch() call instead:

```html
<button type="button" onclick="syncVendor('${brand.vendor_name}')" class="px-3 py-1.5 bg-mpAccent text-white text-sm rounded hover:bg-blue-700 transition">
  Sync Now
</button>

<script>
function syncVendor(name) {
  const btn = event.target;
  btn.disabled = true;
  btn.textContent = 'Syncing...';
  fetch('/internal/vendors/sync/' + encodeURIComponent(name) + '?secret=' + new URLSearchParams(window.location.search).get('secret'), { method: 'POST' })
    .then(r => r.json())
    .then(d => { btn.textContent = d.status === 'started' ? 'Started — refresh in 2min' : 'Error'; })
    .catch(() => { btn.textContent = 'Error'; btn.disabled = false; });
}
</script>
```

Place the script tag once (not per brand) — either at the bottom of the rendered HTML or in a shared block.
  </action>
  <verify>
    <automated>grep -n "runVendorSync" src/routes/vendorAdmin.js && grep -n "sync/:vendorName" src/routes/vendorAdmin.js && grep -n "last_sync_at" src/routes/vendorAdmin.js</automated>
  </verify>
  <acceptance_criteria>
    - `grep 'runVendorSync' src/routes/vendorAdmin.js` returns at least 2 matches (import + usage)
    - `grep 'sync/:vendorName' src/routes/vendorAdmin.js` returns at least 1 match (route definition)
    - `grep 'requireSecret, requirePin' src/routes/vendorAdmin.js` includes a match on the sync route line
    - `grep 'last_sync_at' src/routes/vendorAdmin.js` returns at least 1 match (UI display)
    - `grep 'last_sync_count' src/routes/vendorAdmin.js` returns at least 1 match
    - `grep 'last_sync_error' src/routes/vendorAdmin.js` returns at least 1 match
    - `grep 'Sync Now' src/routes/vendorAdmin.js` returns at least 1 match (button text)
    - The sync route calls `runVendorSync(vendorName).catch(...)` (fire-and-forget with .catch)
    - The import uses `import { runVendorSync } from '../core/vendorSync.js'`
  </acceptance_criteria>
  <done>Platform Console shows sync status (last_sync_at, count, error) per vendor brand and a Sync Now button that triggers the async pipeline via POST /internal/vendors/sync/:vendorName</done>
</task>

</tasks>

<verification>
- `grep -c 'runVendorSync' src/routes/vendorAdmin.js` returns >= 2
- `grep -c 'Sync Now' src/routes/vendorAdmin.js` returns >= 1
- `grep -c 'last_sync_at' src/routes/vendorAdmin.js` returns >= 1
- `npx vitest run` passes
</verification>

<success_criteria>
- Sync Now button visible on Console brand detail page
- POST /internal/vendors/sync/:vendorName requires authentication (requireSecret + requirePin)
- Last sync timestamp and campaign count displayed per brand
- Sync errors displayed in red when present
</success_criteria>

<output>
After completion, create `.planning/phases/01-vendor-sync/01-04-SUMMARY.md`
</output>
