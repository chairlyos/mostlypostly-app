services:
  - type: web
    name: mostlypostly
    env: node
    plan: starter
    buildCommand: "npm install && npx puppeteer browsers install chrome"
    startCommand: "node server.js"
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: 3000
